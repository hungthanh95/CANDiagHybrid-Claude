"""FlexDiag reference Python terminal client (proto=1, TCP transport for M1).

See ``docs/03-TECHNICAL-DETAIL.md`` §8 for the package shape. This is the
reference client: if a capability works here against the Mock ECU, the
Flutter UI only needs UI work.
"""
