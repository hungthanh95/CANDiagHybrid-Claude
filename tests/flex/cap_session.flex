# Diagnostic Session Control capability -- software loopback (Mock ECU)
# Covers: FR session control (capability matrix), proto=1 SESSION verb (docs/03 §1.2).
connect 127.0.0.1 ${PORT}
session 03
bye
