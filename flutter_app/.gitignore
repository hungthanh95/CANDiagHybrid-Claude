# Dart/Flutter build artifacts -- not committed.
.dart_tool/
.packages
build/

# Flutter ephemeral platform files (regenerated by `flutter create`/`flutter
# pub get`; mirrors the standard Flutter app .gitignore).
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
**/doc/api/
linux/flutter/ephemeral/

# IDE
.idea/
*.iml

# Generated for an app project -- not committed for this app either, to
# match the foundation slice's convention (pinned transitive deps are
# regenerated via `flutter pub get`).
pubspec.lock
